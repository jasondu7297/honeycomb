import React from 'react';

  // Title at the top
  const titleStyle = {
    fontSize: '2rem',
    color: '#fff',
    textAlign: 'center',
    marginBottom: '2rem'
  };

const CoeusHeader = () => {
    return <h1 style={titleStyle}>Coeus</h1>
}

export default CoeusHeader;
